{
 "slug": "raii",
 "num": 32,
 "title": "RAII and moves",
 "blurb": "A destructor is a scheduled instruction. Moves are what let ownership be cheap.",
 "part": "systems",
 "partRoman": "VII",
 "partTitle": "Systems",
 "phase": "system",
 "accent": "clay",
 "backend": "godbolt",
 "meta": {
  "needs": [
   "object-model"
  ],
  "minutes": 55,
  "one_idea": "A destructor is a release the compiler schedules on every exit path, and a move is permission to take what a dying object was holding.",
  "sources": [
   "numbers-text-numerics",
   "cpu-architectures"
  ]
 },
 "html": "<p>Every resource has to be released. Memory, file descriptors, locks, sockets, graphics contexts. The hard part has never been remembering to release them once; it is releasing them on every path out of a function, including the ones you did not write.</p>\n<h2 id=\"the-problem-stated-precisely\">The problem, stated precisely</h2>\n<p>A function acquires two resources and can leave through six places: four returns, one exception from a call in the middle, and one exception from a call at the end. Releasing correctly means the right subset released at each of the six, and the number of subsets grows with the number of resources.</p>\n<p>Every language that solved this solved it by taking the decision away from the programmer. Some run a collector later. Some use a defer statement executed at scope exit. C++ ties it to object lifetime, which turns out to be the version with the fewest gaps, because it composes: an object holding two resources releases both without anybody writing a release for the pair.</p>\n<h2 id=\"the-destructor-is-scheduled-not-called\">The destructor is scheduled, not called</h2>\n<p>The key move is that you never write the release at the exit points. You write it once, in a destructor, and the compiler emits a call to it at every point control leaves the scope.</p>\n<p>Every point. The returns, the break, the exception unwinding through. That last one is what a defer statement in a language without exceptions does not have to handle and what a manual release always gets wrong somewhere.</p>\n<p>Destruction happens in reverse order of construction, and this is guaranteed rather than incidental. It has to be: a later object may depend on an earlier one, so the earlier one must outlive it.</p>\n<figure class=\"fig\" data-kind=\"blocks\"><svg viewBox=\"-14 -20 600 162\" class=\"fig-svg\" style=\"max-width:870px\" role=\"img\" aria-label=\"A scope with two objects constructed in order, and four exits from it, each showing both destructors running in reverse order.\" preserveAspectRatio=\"xMidYMid meet\"><defs><marker id=\"fig-arrow\" viewBox=\"0 0 10 10\" refX=\"9\" refY=\"5\" markerWidth=\"7\" markerHeight=\"7\" orient=\"auto-start-reverse\"><path d=\"M0 0 L10 5 L0 10 z\" class=\"fig-head\"/></marker></defs><line x1=\"70\" y1=\"66\" x2=\"70\" y2=\"79\" class=\"fig-arrow\" marker-end=\"url(#fig-arrow)\"/><line x1=\"268\" y1=\"66\" x2=\"484\" y2=\"48\" class=\"fig-arrow\" marker-end=\"url(#fig-arrow)\"/><line x1=\"268\" y1=\"79\" x2=\"484\" y2=\"97\" class=\"fig-arrow\" marker-end=\"url(#fig-arrow)\"/><rect x=\"0.0\" y=\"17.6\" width=\"140.8\" height=\"48.4\" rx=\"4\" class=\"fig-box fig-box-tint\" style=\"fill:var(--jade-ink, var(--accent));stroke:var(--jade-ink, var(--accent))\"/><text x=\"70.4\" y=\"45.8\" font-size=\"12\" text-anchor=\"middle\" class=\"fig-fg\" font-weight=\"600\">construct a</text><rect x=\"0.0\" y=\"79.2\" width=\"140.8\" height=\"48.4\" rx=\"4\" class=\"fig-box fig-box-tint\" style=\"fill:var(--jade-ink, var(--accent));stroke:var(--jade-ink, var(--accent))\"/><text x=\"70.4\" y=\"107.4\" font-size=\"12\" text-anchor=\"middle\" class=\"fig-fg\" font-weight=\"600\">construct b</text><rect x=\"202.4\" y=\"17.6\" width=\"132.0\" height=\"48.4\" rx=\"4\" class=\"fig-box\"/><text x=\"268.4\" y=\"45.8\" font-size=\"12\" text-anchor=\"middle\" class=\"fig-fg\" font-weight=\"600\">return</text><rect x=\"202.4\" y=\"79.2\" width=\"132.0\" height=\"48.4\" rx=\"4\" class=\"fig-box fig-box-tint\" style=\"fill:var(--bad);stroke:var(--bad)\"/><text x=\"268.4\" y=\"107.4\" font-size=\"12\" text-anchor=\"middle\" class=\"fig-fg\" font-weight=\"600\">throw</text><rect x=\"396.0\" y=\"48.4\" width=\"176.0\" height=\"48.4\" rx=\"4\" class=\"fig-box fig-box-tint\" style=\"fill:var(--gold-ink, var(--accent));stroke:var(--gold-ink, var(--accent))\"/><text x=\"484.0\" y=\"76.6\" font-size=\"12\" text-anchor=\"middle\" class=\"fig-fg\" font-weight=\"600\">~b then ~a</text></svg><figcaption>One destructor written, six call sites emitted. The exception path is the one a manual release forgets and the one that matters.</figcaption></figure>\n<h2 id=\"three-promises-a-function-can-make\">Three promises a function can make</h2>\n<p>Once destructors run on the exception path, a function can offer one of three guarantees, and knowing which one you are offering is most of writing correct code with exceptions.</p>\n<p>The basic guarantee says nothing leaks and every object is still in a usable state, though not necessarily the one it had. This is the minimum and it is what RAII gives you almost for free.</p>\n<p>The strong guarantee says the operation either completes or leaves everything exactly as it was. It usually costs a copy: build the new state beside the old one, and swap only when nothing can throw any more.</p>\n<p>The no-throw guarantee says the operation cannot fail. Destructors and swaps must offer it, because they run during unwinding and an exception escaping there ends the program.</p>\n<p>That last rule is why destructors are implicitly <code>noexcept</code> and why a destructor that can fail is a design problem rather than a coding one.</p>\n<h2 id=\"what-a-move-actually-is\">What a move actually is</h2>\n<p>Copying a resource-owning object means duplicating the resource, which is often expensive and sometimes impossible. A socket cannot be copied.</p>\n<p>A move is the alternative: transfer what the source holds and leave the source in a state that is valid and unspecified, so that its destructor runs harmlessly. Measured on a type that counts its own operations, moving a value out of an object leaves the source at zero and the destination holding what it had, with no copy performed.</p>\n<p>Valid and unspecified is the precise phrase and it is worth respecting. A moved-from object may be destroyed and may be assigned to. Reading its value is allowed and tells you nothing. The standard library's types mostly leave themselves empty, and depending on that is depending on an implementation detail.</p>\n<h2 id=\"the-one-that-has-to-be-handled\">The one that has to be handled</h2>\n<p>Assignment is where the special members get interesting, because the target already holds something.</p>\n<p>Copy assignment has to release what the target had, acquire a copy of what the source has, and cope with the two being the same object. Writing it as release then acquire fails on self-assignment: the release destroys the thing about to be copied. The usual answer is copy and swap, which builds the new value first and gives the old one to a temporary that dies at the end of the statement.</p>\n<p>Move assignment has the same shape and one more case. Self-move must leave the object usable, and the standard's requirement is only that it be valid, which is why the library's own types check for it rather than relying on the steal being harmless.</p>\n<p>None of this is an argument for writing them. It is an argument for the rule that follows: every one of these hazards belongs to a class that owns something raw, and a class that owns nothing raw has none of them.</p>\n<h2 id=\"the-cast-that-does-not-move\">The cast that does not move</h2>\n<p><code>std::move</code> moves nothing. It is a cast, and its entire body is a <code>static_cast</code> to an rvalue reference.</p>\n<p>Measured: calling <code>std::move</code> on an object and binding the result to a reference performs zero moves and zero copies. What performs the move is the constructor or assignment that the cast made eligible for overload resolution.</p>\n<p>Which means <code>std::move</code> on a <code>const</code> object silently does nothing useful. The cast produces a <code>const</code> rvalue reference, no move constructor accepts one, the copy constructor is chosen instead, and you get a copy where you asked for a move, with no diagnostic.</p>\n<h2 id=\"the-move-that-never-happens\">The move that never happens</h2>\n<p>Returning a local object looks like it should cost a move. It does not.</p>\n<p>Measured on the same counting type: a function returning a temporary performs one construction and one destruction. A function that names a local and returns it performs one construction and one destruction. Not a move, not a copy.</p>\n<p>Since C++17 this is guaranteed for the temporary case rather than merely allowed: the object is constructed directly in the caller's storage, and there is no second object for a move to happen between. The named case is still technically an optimisation and every compiler performs it.</p>\n<p>The practical rule that follows is the one people get backwards. Do not write <code>return std::move(local)</code>. It cannot help, because there was nothing to elide the move of, and it can hurt, because naming a cast where a plain name was expected prevents the elision that would otherwise happen.</p>\n<h2 id=\"where-the-scope-is-not-the-lifetime\">Where the scope is not the lifetime</h2>\n<p>RAII binds a release to a scope, and sometimes the scope is not where the resource should die. Three answers, in increasing cost.</p>\n<p>Move it out. A function that opens something and returns the handle transfers ownership to the caller, and the release now happens at the caller's scope exit. This is what the move is for and it costs nothing.</p>\n<p>Put it in a container. A vector of owning objects releases all of them when it goes, and the individual lifetimes are the container's problem.</p>\n<p>Share it. When two independent parts genuinely both need the thing alive, a reference count decides who is last. That is the only case that needs <code>shared_ptr</code>, and reaching for it before establishing that ownership really is shared is the commonest way a C++ codebase becomes hard to reason about: with everything shared, nothing has an owner and no lifetime is stated anywhere.</p>\n<p>The question worth asking at each resource is who decides when this dies. If the answer is one place, it is exclusive. If the answer is whoever finishes last, it is shared. If the answer is unclear, the design is unclear, and no smart pointer fixes that.</p>\n<h2 id=\"zero-or-five\">Zero, or five</h2>\n<p>If a class owns a raw resource it needs a destructor, and then the compiler's generated copy operations are wrong: they duplicate the pointer, both objects destroy it, and the second destruction is a double free.</p>\n<p>So the rule of five: if you write any of the destructor, copy constructor, copy assignment, move constructor or move assignment, you probably need all five.</p>\n<p>And the rule of zero, which is the better one: do not own raw resources. Hold a type that already handles it, and write none of the five, and let the compiler generate all of them correctly.</p>\n<p>Measured, the cost of that advice is nothing. A <code>unique_ptr</code> is 8 bytes, the same as the raw pointer it replaces. A <code>shared_ptr</code> is 16, because it also carries a pointer to the reference counts, and its copies cost an atomic increment, which unit 026 already priced.</p>\n<h2 id=\"what-to-carry-forward\">What to carry forward</h2>\n<p>A destructor is a release the compiler schedules at every exit, including the ones exceptions take, and destruction is in reverse construction order because later objects may depend on earlier ones.</p>\n<p>A move transfers and leaves the source valid and unspecified. <code>std::move</code> is a cast that makes a move eligible and performs none itself, and returning a local performs neither a copy nor a move.</p>\n<p>Own nothing raw and you write none of the five special members. That costs zero bytes for exclusive ownership and one pointer plus an atomic for shared.</p>\n<h2 id=\"reading-the-errors-you-are-about-to-see\">Reading the errors you are about to see</h2>\n<p>These count constructions, moves and destructions with static counters, which is exact and reproducible. Every expected count was produced by running the code.</p>\n<p><code>assert-failed</code> names the count that disagreed. Several of these exercises assert that something does not happen, which is the point: an assertion that a move occurred where the language guarantees elision would be testing the wrong claim.</p>",
 "headings": [
  {
   "id": "the-problem-stated-precisely",
   "text": "The problem, stated precisely",
   "level": 2
  },
  {
   "id": "the-destructor-is-scheduled-not-called",
   "text": "The destructor is scheduled, not called",
   "level": 2
  },
  {
   "id": "three-promises-a-function-can-make",
   "text": "Three promises a function can make",
   "level": 2
  },
  {
   "id": "what-a-move-actually-is",
   "text": "What a move actually is",
   "level": 2
  },
  {
   "id": "the-one-that-has-to-be-handled",
   "text": "The one that has to be handled",
   "level": 2
  },
  {
   "id": "the-cast-that-does-not-move",
   "text": "The cast that does not move",
   "level": 2
  },
  {
   "id": "the-move-that-never-happens",
   "text": "The move that never happens",
   "level": 2
  },
  {
   "id": "where-the-scope-is-not-the-lifetime",
   "text": "Where the scope is not the lifetime",
   "level": 2
  },
  {
   "id": "zero-or-five",
   "text": "Zero, or five",
   "level": 2
  },
  {
   "id": "what-to-carry-forward",
   "text": "What to carry forward",
   "level": 2
  },
  {
   "id": "reading-the-errors-you-are-about-to-see",
   "text": "Reading the errors you are about to see",
   "level": 2
  }
 ],
 "words": 1548,
 "needs": [
  "object-model"
 ],
 "neededBy": [
  "compile-time"
 ]
}
