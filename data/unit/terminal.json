{
 "slug": "terminal",
 "num": 44,
 "title": "The terminal is a kernel object",
 "blurb": "Not the program and not the window: a kernel state machine with settable flags. Every weird behaviour is that machine obeying.",
 "part": "tools",
 "partRoman": "IX",
 "partTitle": "Tools",
 "phase": "system",
 "accent": "clay",
 "backend": "godbolt",
 "meta": {
  "needs": [
   "syscalls",
   "processes"
  ],
  "minutes": 55,
  "one_idea": "A terminal is a kernel device with settable flags sitting between your keyboard and your program, and every behaviour people call weird is that device obeying its current settings.",
  "sources": [
   "compilers-interpreters-terminals-unix"
  ]
 },
 "html": "<p>Everyone treats the terminal as a program: the window, the thing with the font in it. That is the emulator. The terminal your code talks to is a kernel object with a driver, a buffer and about thirty flags, and it sits between the keyboard and your process doing work you did not ask for and cannot see.</p>\n<p>Once you know the flags, the behaviour stops being folklore. Every measurement below came from opening a pseudoterminal and asking it.</p>\n<h2 id=\"two-ends-of-one-device\">Two ends of one device</h2>\n<p>A pseudoterminal is a pair. One end, the master, is held by whatever is drawing the window or carrying the connection. The other, the slave, is the file your program has on descriptors 0, 1 and 2.</p>\n<p>Between them sits the line discipline, which is kernel code, not code in either program. It holds a buffer, it edits that buffer when you press backspace, it copies characters back towards the screen, and it turns certain bytes into signals. None of that is in the emulator and none of it is in your program.</p>\n<p>So there are three participants and people usually think there are two. When a program behaves strangely at the terminal it is almost always the middle one doing exactly what its flags say.</p>\n<figure class=\"fig\" data-kind=\"blocks\"><svg viewBox=\"-14 -20 635 153\" class=\"fig-svg\" style=\"max-width:921px\" role=\"img\" aria-label=\"A keyboard feeding the master end of a pseudoterminal, the line discipline in the kernel between master and slave, and a program reading the slave end.\" preserveAspectRatio=\"xMidYMid meet\"><defs><marker id=\"fig-arrow\" viewBox=\"0 0 10 10\" refX=\"9\" refY=\"5\" markerWidth=\"7\" markerHeight=\"7\" orient=\"auto-start-reverse\"><path d=\"M0 0 L10 5 L0 10 z\" class=\"fig-head\"/></marker></defs><line x1=\"123\" y1=\"95\" x2=\"158\" y2=\"95\" class=\"fig-arrow\" marker-end=\"url(#fig-arrow)\"/><line x1=\"282\" y1=\"95\" x2=\"317\" y2=\"95\" class=\"fig-arrow\" marker-end=\"url(#fig-arrow)\"/><line x1=\"449\" y1=\"95\" x2=\"484\" y2=\"95\" class=\"fig-arrow\" marker-end=\"url(#fig-arrow)\"/><rect x=\"0.0\" y=\"70.4\" width=\"123.2\" height=\"48.4\" rx=\"4\" class=\"fig-box fig-box-tint\" style=\"fill:var(--copper-ink, var(--accent));stroke:var(--copper-ink, var(--accent))\"/><text x=\"61.6\" y=\"98.6\" font-size=\"12\" text-anchor=\"middle\" class=\"fig-fg\" font-weight=\"600\">keyboard</text><rect x=\"158.4\" y=\"70.4\" width=\"123.2\" height=\"48.4\" rx=\"4\" class=\"fig-box fig-box-tint\" style=\"fill:var(--azure-ink, var(--accent));stroke:var(--azure-ink, var(--accent))\"/><text x=\"220.0\" y=\"98.6\" font-size=\"12\" text-anchor=\"middle\" class=\"fig-fg\" font-weight=\"600\">master</text><rect x=\"316.8\" y=\"70.4\" width=\"132.0\" height=\"48.4\" rx=\"4\" class=\"fig-box fig-box-tint\" style=\"fill:var(--gold-ink, var(--accent));stroke:var(--gold-ink, var(--accent))\"/><text x=\"382.8\" y=\"98.6\" font-size=\"12\" text-anchor=\"middle\" class=\"fig-fg\" font-weight=\"600\">line discipline</text><rect x=\"484.0\" y=\"70.4\" width=\"123.2\" height=\"48.4\" rx=\"4\" class=\"fig-box fig-box-tint\" style=\"fill:var(--jade-ink, var(--accent));stroke:var(--jade-ink, var(--accent))\"/><text x=\"545.6\" y=\"98.6\" font-size=\"12\" text-anchor=\"middle\" class=\"fig-fg\" font-weight=\"600\">your read</text></svg><figcaption>The line discipline is kernel code between the two ends. It buffers a line, echoes what you type, and converts bytes into signals, none of which either program did.</figcaption></figure>\n<h2 id=\"why-your-read-does-not-return\">Why your read does not return</h2>\n<p>Open a fresh pseudoterminal and the flag <code>ICANON</code> is set. That is canonical mode, and it means the line discipline collects bytes until it sees a newline before letting any of them through.</p>\n<p>Write <code>hello</code> into the master end and then ask whether the slave has anything readable. It does not. Write one more byte, a newline, and the slave becomes readable and returns <code>hello\\n</code>, all six bytes at once.</p>\n<p>Which explains the thing every beginner hits. A program that wants a single keypress writes a read, presses a key, and nothing happens. The read is correct and the key was delivered. It is sitting in a kernel buffer waiting for a newline, and the program will not see it until one arrives.</p>\n<p>Canonical mode is also where line editing lives. Backspace works before your program runs because the discipline is holding the line and can take a character back off it. The byte it treats as erase is 0x7f on this machine, which is delete rather than backspace, and that mismatch is the origin of a whole genre of terminal configuration.</p>\n<h2 id=\"the-characters-you-did-not-print\">The characters you did not print</h2>\n<p>Open another pseudoterminal, write <code>abc\\n</code> into the master, and start no program at all. Read the master back and you get <code>abc\\r\\n</code>.</p>\n<p>Nobody printed that. There is no process on the slave end. The echo is the line discipline copying input back towards the display because <code>ECHO</code> is set, and it happens whether or not anything is reading.</p>\n<p>That is why a password prompt has to turn a flag off rather than avoid printing something. The program was never printing the password in the first place. It clears <code>ECHO</code>, reads, and sets it back, and if it crashes in between your shell stays silent until you fix it by hand.</p>\n<h2 id=\"enter-is-not-a-newline\">Enter is not a newline</h2>\n<p>The key sends carriage return, 0x0D. Programs expect line feed, 0x0A. The conversion is another flag.</p>\n<p>Write 0x0D into the master and read the slave: it returns 0x0A. That is <code>ICRNL</code>, which is on by default, translating input carriage returns to newlines.</p>\n<p>The reverse happens on output. A program writes <code>line\\n</code>, five bytes ending in 0x0A, and the master receives <code>line\\r\\n</code>, six bytes. That is <code>ONLCR</code> under <code>OPOST</code>, translating a newline into carriage return and newline on the way out.</p>\n<p>So the terminal you are talking to speaks carriage return and your program speaks newline, and neither of them knows, because the kernel is translating in both directions. Turn the output side off and every line after the first starts where the previous one ended, which is the staircase effect people recognise without knowing its cause.</p>\n<h2 id=\"the-interrupt-that-is-a-byte-first\">The interrupt that is a byte first</h2>\n<p>Pressing Ctrl-C sends the byte 0x03. Nothing about that byte is special to the hardware or to your program.</p>\n<p>The line discipline holds a table of control characters, and on this machine the entry for interrupt is 0x03, for end of file 0x04, for suspend 0x1a and for quit 0x1c. When <code>ISIG</code> is set and one of those bytes arrives, the discipline raises the corresponding signal at the foreground process group rather than delivering the byte.</p>\n<p>Both halves are measurable. Give a child a controlling terminal with the default flags, write 0x03, and the child dies of signal 2, which is <code>SIGINT</code>. Clear <code>ISIG</code> on the same terminal, write the same byte, and the child reads it: an ordinary byte with value 3, and no signal anywhere.</p>\n<p>Which is why Ctrl-C does not work in some programs. They cleared the flag, so the byte reaches them and they decide what it means. And it is why Ctrl-C reaches a program that ignored <code>SIGINT</code> as nothing at all: the discipline raised the signal correctly and the target threw it away.</p>\n<h2 id=\"turning-all-of-it-off\">Turning all of it off</h2>\n<p>Raw mode is not a mode in the kernel. It is a name for clearing the flags above in one go.</p>\n<p>Put a pseudoterminal into raw mode and measure what changed: <code>ICANON</code> cleared, <code>ECHO</code> cleared, <code>ISIG</code> cleared, <code>ICRNL</code> cleared, and <code>OPOST</code> cleared, which disables the output translation as a whole. Write one byte with no newline and the read returns immediately with that byte. Have the program write <code>line\\n</code> and the master receives exactly five bytes, with no carriage return added.</p>\n<p>That is the deal an editor or a full-screen program takes. It gets every keystroke the instant it arrives, and in exchange it owns everything: echoing what you type, handling backspace, noticing that 0x03 means you want out, and emitting carriage returns itself. What it draws with that control is escape sequences, which are ordinary bytes the emulator interprets rather than prints.</p>\n<p>Two more settings decide what immediately means. <code>VMIN</code> and <code>VTIME</code> are one and zero by default, which says a read returns as soon as one byte exists and never times out. Raising <code>VMIN</code> makes a read wait for that many bytes, and setting <code>VTIME</code> gives it a deadline in tenths of a second, which is how a program distinguishes an escape key pressed alone from an escape sequence still arriving.</p>\n<h2 id=\"where-the-size-of-the-window-lives\">Where the size of the window lives</h2>\n<p>The width and height are not the emulator's private business and they are not an environment variable, whatever your shell suggests. They are fields in the kernel device.</p>\n<p>A fresh pseudoterminal reports zero rows and zero columns, because nothing has told it. Set 40 rows by 132 columns on the master and ask the slave: it reports 40 by 132. One number, two ends, held between them by the driver.</p>\n<p>That is the mechanism behind resizing. The emulator sets the new size on its end, the kernel stores it and raises <code>SIGWINCH</code> at the foreground process group, and a program that cares asks the device for the new value. A program that does not care carries on drawing at the old width, which is why some output stays broken until you rerun it.</p>\n<h2 id=\"what-to-carry-forward\">What to carry forward</h2>\n<p>The terminal your program talks to is a kernel device, and the line discipline between the two ends is doing the buffering, the echoing, the translation and the signal generation.</p>\n<p>Canonical mode holds your input until a newline, which is why a read for one keypress does not return. Echo happens with no program involved. Input carriage returns become newlines and output newlines become carriage return and newline, in the kernel, in both directions.</p>\n<p>Ctrl-C is the byte 0x03 turned into a signal by a flag that can be cleared, and raw mode is clearing that flag along with the rest. The window size is a pair of numbers in the driver, and a signal tells you they changed.</p>\n<h2 id=\"reading-the-errors-you-are-about-to-see\">Reading the errors you are about to see</h2>\n<p>These model the line discipline rather than opening a real terminal, because a test that needs a controlling terminal is a test that does not run under most build systems.</p>\n<p><code>assert-failed</code> names the behaviour your model got wrong. Several exercises assert that a read returns nothing, which is the point of canonical mode rather than a hang in the test.</p>",
 "headings": [
  {
   "id": "two-ends-of-one-device",
   "text": "Two ends of one device",
   "level": 2
  },
  {
   "id": "why-your-read-does-not-return",
   "text": "Why your read does not return",
   "level": 2
  },
  {
   "id": "the-characters-you-did-not-print",
   "text": "The characters you did not print",
   "level": 2
  },
  {
   "id": "enter-is-not-a-newline",
   "text": "Enter is not a newline",
   "level": 2
  },
  {
   "id": "the-interrupt-that-is-a-byte-first",
   "text": "The interrupt that is a byte first",
   "level": 2
  },
  {
   "id": "turning-all-of-it-off",
   "text": "Turning all of it off",
   "level": 2
  },
  {
   "id": "where-the-size-of-the-window-lives",
   "text": "Where the size of the window lives",
   "level": 2
  },
  {
   "id": "what-to-carry-forward",
   "text": "What to carry forward",
   "level": 2
  },
  {
   "id": "reading-the-errors-you-are-about-to-see",
   "text": "Reading the errors you are about to see",
   "level": 2
  }
 ],
 "words": 1427,
 "needs": [
  "syscalls",
  "processes"
 ],
 "neededBy": [
  "shell"
 ]
}
