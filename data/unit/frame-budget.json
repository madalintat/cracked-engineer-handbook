{
 "slug": "frame-budget",
 "num": 85,
 "title": "Why a screen is an arithmetic problem",
 "blurb": "Huge, deadlined and independent. A 16-core CPU has 80 cycles per pixel for everything.",
 "part": "gpu",
 "partRoman": "XVI",
 "partTitle": "Graphics and the GPU",
 "phase": "parallel",
 "accent": "jade",
 "backend": "godbolt",
 "meta": {
  "needs": [
   "pipeline",
   "cache"
  ],
  "minutes": 55,
  "one_idea": "Rendering is huge, deadlined and independent, and once you do the arithmetic the shape of a graphics processor stops being a design choice and becomes the only answer that fits in the time.",
  "sources": [
   "graphics-pipeline"
  ]
 },
 "html": "<p>A screen is an arithmetic problem before it is anything else. Work out how much has to happen and how long there is to do it in, and most of what a graphics processor looks like follows from the numbers rather than from anybody's taste.</p>\n<p>Three properties make this workload what it is. It is huge: millions of outputs per frame. It is deadlined: late is the same as wrong, because a frame that misses its slot is a stutter somebody sees. And it is independent: no pixel needs to know what any other pixel decided.</p>\n<p>That last one is the rare property. Almost nothing else in this handbook has it, and everything about the machine in the next ten units is built on it.</p>\n<h2 id=\"one-nanosecond-per-pixel\">One nanosecond per pixel</h2>\n<p>Take a display of 3840 by 2160 at 120 frames a second.</p>\n<p>That is 8,294,400 pixels per frame, and just under a billion pixels a second. The frame budget is one hundred and twentieth of a second, which is 8.333 milliseconds. Divide one by the other:</p>\n<pre class=\"cb\" data-lang=\"text\"><code>8.333 ms / 8,294,400 px  =  about 1.0 nanoseconds per pixel</code></pre>\n<p>One nanosecond, per pixel, for everything: transforming the geometry, working out which pixels a triangle covers, running the shader, fetching the textures, testing the depth, blending the result. On a five gigahertz core a nanosecond is five clock cycles.</p>\n<p>That number is the whole unit. Every design decision downstream is an answer to it.</p>\n<h2 id=\"except-each-pixel-is-not-drawn-once\">Except each pixel is not drawn once</h2>\n<p>Geometry is submitted before anything knows what will end up visible, so a pixel is written, then covered by something nearer, then covered again. The ratio of fragments produced to pixels finally shown is overdraw, and for opaque geometry a factor of two to four is normal. Transparency is worse: particles, foliage and interface layers can push a local region to ten or more.</p>\n<p>Take three as a working figure and the arithmetic moves:</p>\n<pre class=\"cb\" data-lang=\"text\"><code>8,294,400 px x 3       =  24,883,200 fragments per frame\nx 120 frames           =  about 3 billion fragment shader runs per second</code></pre>\n<p>Now attach a shader to each one. A cheap textured fragment is tens of arithmetic operations. A modest one with a few lights, a normal map and a reflectance model is a few hundred. At two hundred operations per fragment:</p>\n<pre class=\"cb\" data-lang=\"text\"><code>24,883,200 x 200       =  about 5 billion operations per frame\nx 120 frames           =  about 600 billion operations per second</code></pre>\n<p>That is fragment shading alone, at a shader nobody would call expensive. A serious deferred lighting pass at a thousand operations per fragment puts it at three trillion. Then add vertex work, shadow maps, which redo the geometry once per light, and every post-processing pass.</p>\n<h2 id=\"where-the-rest-of-the-silicon-goes\">Where the rest of the silicon goes</h2>\n<p>Two more budgets are worth doing, because each one explains a piece of hardware that would otherwise look arbitrary.</p>\n<p>Texturing first. Say eight samples per fragment, which is unremarkable: colour, normal, roughness, metalness, ambient occlusion, a couple of shadow taps and an environment probe. Each of those, filtered smoothly, reads eight texels.</p>\n<pre class=\"cb\" data-lang=\"text\"><code>24,883,200 x 8 x 8     =  about 1.6 billion texel reads per frame\nx 120 frames           =  about 190 billion per second</code></pre>\n<p>Every one of those needs an address computed with wrapping, a format decoded and a weighted blend across the neighbours. Nobody writes that as shader code at 190 billion a second, which is why filtering is fixed function silicon and has been since the beginning.</p>\n<p>Then the framebuffer. Per fragment the output stage reads the depth, usually writes it, and writes a colour, and for anything blended reads the colour too. Call it twelve bytes:</p>\n<pre class=\"cb\" data-lang=\"text\"><code>24,883,200 x 12 B      =  about 300 MB per frame\nx 120 frames           =  about 36 GB per second</code></pre>\n<p>That is the framebuffer alone, with no textures in it at all. Add texture traffic and you are into hundreds of gigabytes per second, which is what the memory on a discrete card is built to deliver. A phone has a small fraction of that and shares it with the processor, which is the entire reason mobile graphics hardware is built differently.</p>\n<h2 id=\"the-honest-comparison-with-a-processor\">The honest comparison with a processor</h2>\n<p>The unfair version of this argument compares a graphics part with one processor core and declares a thousandfold gap. Do it properly instead.</p>\n<p>A sixteen core processor at five gigahertz with two wide fused multiply add units per core peaks at roughly five trillion operations a second. A large graphics card is about eighty. The gap is around sixteen times, not a thousand. A processor is not hopeless at raw arithmetic.</p>\n<p>It is hopeless at this job, for four reasons that have nothing to do with peak arithmetic.</p>\n<p>The first is the per pixel budget. That same processor has sixteen times five billion times 8.333 milliseconds, which is 666 million core cycles in a frame. Spread over the pixels:</p>\n<pre class=\"cb\" data-lang=\"text\"><code>666,000,000 / 8,294,400  =  about 80 cycles per pixel\nwith 3x overdraw         =  about 27 cycles per fragment</code></pre>\n<p>Twenty seven cycles. One smoothly filtered texture read is four dependent loads, each likely to miss a cache and cost tens to hundreds of cycles, plus the blends between them. A single texture sample spends the entire per fragment budget, and a real shader wants eight of them, before any of the triangle setup, coverage, interpolation, depth test or blend has happened.</p>\n<figure class=\"fig\" data-kind=\"blocks\"><svg viewBox=\"-14 -20 530 179\" class=\"fig-svg\" style=\"max-width:768px\" role=\"img\" aria-label=\"A per fragment budget of twenty seven processor cycles set against the cost of one filtered texture read, which is four dependent memory loads plus blending and exceeds the whole budget by itself.\" preserveAspectRatio=\"xMidYMid meet\"><defs><marker id=\"fig-arrow\" viewBox=\"0 0 10 10\" refX=\"9\" refY=\"5\" markerWidth=\"7\" markerHeight=\"7\" orient=\"auto-start-reverse\"><path d=\"M0 0 L10 5 L0 10 z\" class=\"fig-head\"/></marker></defs><line x1=\"202\" y1=\"24\" x2=\"282\" y2=\"24\" class=\"fig-arrow\" marker-end=\"url(#fig-arrow)\"/><line x1=\"392\" y1=\"48\" x2=\"392\" y2=\"97\" class=\"fig-arrow\" marker-end=\"url(#fig-arrow)\"/><rect x=\"0.0\" y=\"0.0\" width=\"202.4\" height=\"48.4\" rx=\"4\" class=\"fig-box fig-box-tint\" style=\"fill:var(--gold-ink, var(--accent));stroke:var(--gold-ink, var(--accent))\"/><text x=\"101.2\" y=\"28.2\" font-size=\"12\" text-anchor=\"middle\" class=\"fig-fg\" font-weight=\"600\">27 cycles per fragment</text><rect x=\"281.6\" y=\"0.0\" width=\"220.0\" height=\"48.4\" rx=\"4\" class=\"fig-box fig-box-tint\" style=\"fill:var(--clay-ink, var(--accent));stroke:var(--clay-ink, var(--accent))\"/><text x=\"391.6\" y=\"28.2\" font-size=\"12\" text-anchor=\"middle\" class=\"fig-fg\" font-weight=\"600\">1 filtered sample</text><rect x=\"281.6\" y=\"96.8\" width=\"220.0\" height=\"48.4\" rx=\"4\" class=\"fig-box fig-box-tint\" style=\"fill:var(--clay-ink, var(--accent));stroke:var(--clay-ink, var(--accent))\"/><text x=\"391.6\" y=\"125.0\" font-size=\"12\" text-anchor=\"middle\" class=\"fig-fg\" font-weight=\"600\">4 dependent loads</text></svg><figcaption>The budget is not tight, it is already spent. One filtered texture read costs more than the whole per fragment allowance, and a real shader asks for eight of them.</figcaption></figure>\n<p>The second is that everything the graphics hardware does in fixed function costs a processor tens of instructions. Filtering is a pipelined array producing a result per clock; in software it is address arithmetic, four gathers, format conversion and a blend tree. A depth test and blend is one read modify write in a dedicated unit; in software it is a load, a compare, a branch, a blend and a store, with the next fragment hitting the same pixel right behind it.</p>\n<p>The third is that the memory system is built for the wrong thing. A processor cache hierarchy exists to make one thread's next access fast, and to keep shared data coherent between cores. A renderer wants aggregate throughput over an access pattern with almost no reuse, and no two fragments share anything, so the coherence machinery is pure cost.</p>\n<p>The fourth is the deepest. A processor hides memory latency by being clever about one instruction stream: out of order execution, a large reorder buffer, branch prediction, an enormous transistor investment to find a handful of independent instructions inside a sequence written to be sequential. A renderer does not have one stream. It has millions of them, already independent, sitting in a queue. The right answer is not cleverness about one; it is to keep hundreds of them resident and switch between them for nothing.</p>\n<p>So the answer to why a processor cannot render is not that it lacks arithmetic. It is that it spends its transistors on a problem this workload does not have, because it was designed for work where independence is scarce, and here independence is free.</p>\n<h2 id=\"what-to-carry-forward\">What to carry forward</h2>\n<p>Rendering is huge, deadlined and independent, and the last of those is what makes a different machine possible.</p>\n<p>At four thousand pixels wide and 120 frames a second there is about one nanosecond per pixel for everything, which is five cycles of a fast core. Overdraw multiplies the work by three or more before a shader has run once.</p>\n<p>Texture filtering is fixed function because nobody executes 190 billion filtered reads a second as instructions. Framebuffer traffic alone is tens of gigabytes per second, which is why compression of depth and colour is mandatory rather than clever, and why a phone with shared memory needs a different architecture.</p>\n<p>A processor is about sixteen times slower at peak arithmetic and infinitely worse at this job, because eighty cycles per pixel does not buy one texture sample, and because it hides latency with a mechanism that assumes parallelism is scarce.</p>\n<p>Next is where those fragments come from: how three edges and a grid turn a triangle into pixels, and why the answer arrives in blocks of four.</p>\n<h2 id=\"reading-the-errors-you-are-about-to-see\">Reading the errors you are about to see</h2>\n<p>These are the arithmetic of the unit, done in integers. Rates are large, so the tests use 64 bit types throughout and the intermediate values matter.</p>\n<p><code>assert-failed</code> names the case your model got wrong. Several exercises assert results that overflow a 32 bit integer, which is the point rather than an accident: a billion pixels a second does not fit in the type most people reach for first.</p>",
 "headings": [
  {
   "id": "one-nanosecond-per-pixel",
   "text": "One nanosecond per pixel",
   "level": 2
  },
  {
   "id": "except-each-pixel-is-not-drawn-once",
   "text": "Except each pixel is not drawn once",
   "level": 2
  },
  {
   "id": "where-the-rest-of-the-silicon-goes",
   "text": "Where the rest of the silicon goes",
   "level": 2
  },
  {
   "id": "the-honest-comparison-with-a-processor",
   "text": "The honest comparison with a processor",
   "level": 2
  },
  {
   "id": "what-to-carry-forward",
   "text": "What to carry forward",
   "level": 2
  },
  {
   "id": "reading-the-errors-you-are-about-to-see",
   "text": "Reading the errors you are about to see",
   "level": 2
  }
 ],
 "words": 1445,
 "needs": [
  "cache",
  "pipeline"
 ],
 "neededBy": []
}
