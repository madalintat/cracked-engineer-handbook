{
 "slug": "page-cache",
 "num": 41,
 "title": "The kernel's memory of the disk",
 "blurb": "Your reads and writes talk to RAM. Durability is the separate, expensive act of leaving it.",
 "part": "storage",
 "partRoman": "VIII",
 "partTitle": "Storage",
 "phase": "system",
 "accent": "clay",
 "backend": "godbolt",
 "meta": {
  "needs": [
   "virtual-memory",
   "flash"
  ],
  "minutes": 55,
  "one_idea": "Your reads and writes talk to memory, and durability is a separate act you have to ask for and check.",
  "sources": [
   "cpu-architectures",
   "compilers-interpreters-terminals-unix"
  ]
 },
 "html": "<p>A program calls <code>write</code>, the call returns, and the program continues. Nothing reached the disk. Something eventually will, on a schedule nobody in the program chose, and the gap between those two facts is where a whole category of data loss lives.</p>\n<h2 id=\"the-cache-is-the-memory-system\">The cache is the memory system</h2>\n<p>The kernel keeps file contents in memory, in pages, in the same structures unit 035 described. A file's pages and a process's anonymous pages compete for the same physical memory under the same replacement policy, which is why a machine copying a large file evicts a program's data to make room and then appears to have less memory than it did.</p>\n<p>A read consults the cache. A hit is a copy from memory, which is tens of nanoseconds a page. A miss is a major fault, which is a device access, and unit 039 priced that in tens of microseconds. Three orders of magnitude, decided by whether somebody read it recently.</p>\n<p>A write marks a page dirty and returns. The data is in memory, the call succeeded, and nothing has been sent anywhere.</p>\n<figure class=\"fig\" data-kind=\"blocks\"><svg viewBox=\"-14 -20 565 197\" class=\"fig-svg\" style=\"max-width:819px\" role=\"img\" aria-label=\"A write landing in a dirty page in memory, returning immediately, with a separate writeback path to the device and a separate flush the application must request.\" preserveAspectRatio=\"xMidYMid meet\"><defs><marker id=\"fig-arrow\" viewBox=\"0 0 10 10\" refX=\"9\" refY=\"5\" markerWidth=\"7\" markerHeight=\"7\" orient=\"auto-start-reverse\"><path d=\"M0 0 L10 5 L0 10 z\" class=\"fig-head\"/></marker></defs><line x1=\"132\" y1=\"86\" x2=\"176\" y2=\"86\" class=\"fig-arrow\" marker-end=\"url(#fig-arrow)\"/><line x1=\"251\" y1=\"62\" x2=\"458\" y2=\"57\" class=\"fig-arrow\" marker-end=\"url(#fig-arrow)\"/><line x1=\"251\" y1=\"110\" x2=\"458\" y2=\"114\" class=\"fig-arrow\" marker-end=\"url(#fig-arrow)\"/><rect x=\"0.0\" y=\"61.6\" width=\"132.0\" height=\"48.4\" rx=\"4\" class=\"fig-box fig-box-tint\" style=\"fill:var(--azure-ink, var(--accent));stroke:var(--azure-ink, var(--accent))\"/><text x=\"66.0\" y=\"89.8\" font-size=\"12\" text-anchor=\"middle\" class=\"fig-fg\" font-weight=\"600\">write</text><rect x=\"176.0\" y=\"61.6\" width=\"149.6\" height=\"48.4\" rx=\"4\" class=\"fig-box fig-box-tint\" style=\"fill:var(--gold-ink, var(--accent));stroke:var(--gold-ink, var(--accent))\"/><text x=\"250.8\" y=\"89.8\" font-size=\"12\" text-anchor=\"middle\" class=\"fig-fg\" font-weight=\"600\">dirty page</text><rect x=\"378.4\" y=\"8.8\" width=\"158.4\" height=\"48.4\" rx=\"4\" class=\"fig-box\"/><text x=\"457.6\" y=\"32.0\" font-size=\"12\" text-anchor=\"middle\" class=\"fig-fg\" font-weight=\"600\">writeback</text><text x=\"457.6\" y=\"48.0\" font-size=\"10\" text-anchor=\"middle\" class=\"fig-dim\">eventually</text><rect x=\"378.4\" y=\"114.4\" width=\"158.4\" height=\"48.4\" rx=\"4\" class=\"fig-box fig-box-tint\" style=\"fill:var(--jade-ink, var(--accent));stroke:var(--jade-ink, var(--accent))\"/><text x=\"457.6\" y=\"137.6\" font-size=\"12\" text-anchor=\"middle\" class=\"fig-fg\" font-weight=\"600\">flush</text><text x=\"457.6\" y=\"153.6\" font-size=\"10\" text-anchor=\"middle\" class=\"fig-dim\">when you ask</text></svg><figcaption>Two paths and only one of them is yours. The write returns when the page is dirty; whether it ever reaches the device is a different question with a different answer.</figcaption></figure>\n<h2 id=\"guessing-what-comes-next\">Guessing what comes next</h2>\n<p>The kernel watches the offsets a file is read at, and when they look sequential it fetches ahead.</p>\n<p>This is the same idea as the hardware prefetcher from unit 024 at a different scale, with the same consequence: sequential access is far faster than the device latency suggests, because the data arrived before it was asked for, and random access gets no help at all.</p>\n<p>The window grows while the guess keeps being right and collapses when it stops. So a program that reads a file sequentially and occasionally jumps gets most of the benefit, and one that jumps constantly disables the mechanism and pays full latency per access.</p>\n<p>Which is worth knowing because it is adjustable. A program that knows it will read a file straight through can say so, and one that knows its access is random can say that too and stop the kernel reading pages it will never use.</p>\n<h2 id=\"when-a-write-finally-blocks\">When a write finally blocks</h2>\n<p>Dirty pages accumulate and something has to bound them, because memory full of unwritten data is memory that cannot be reclaimed and data that cannot be recovered.</p>\n<p>The kernel has two thresholds. Above the first, background writeback starts and your program notices nothing. Above the second, a process that dirties another page is made to write some out itself, and at that moment <code>write</code> stops returning immediately and starts taking as long as the device does.</p>\n<p>That transition is abrupt and it is the explanation for a common shape: a program that writes at memory speed for several seconds and then suddenly at device speed forever. Nothing changed in the program. It filled the allowance.</p>\n<h2 id=\"the-only-thing-that-makes-it-durable\">The only thing that makes it durable</h2>\n<p><code>fsync</code> is the primitive, and what it promises is narrow enough to be worth stating exactly.</p>\n<p>It writes back every dirty page of that file and asks the device to flush its own buffer, which is the flush unit 039 described. When it returns successfully, the data is on the medium.</p>\n<p>It does not make anything else durable. In particular it says nothing about the directory the file is in, so a newly created file can be fully written and flushed and still not exist after a power loss, because the directory entry naming it was itself a dirty page nobody flushed.</p>\n<p>The safe pattern for replacing a file is four steps and every one is needed: write the new contents to a temporary file, flush it, rename it over the original, and flush the directory. Skip the last and the rename is not durable. Skip the second and the file exists with the wrong contents, which is worse than not existing.</p>\n<h2 id=\"the-error-you-only-get-once\">The error you only get once</h2>\n<p>This one is a genuine trap and it cost a well-known database several years of quiet corruption.</p>\n<p>If writeback fails, the kernel has a problem: the failure happened asynchronously, long after the <code>write</code> that caused it returned successfully, and there is nobody obvious to tell. What it does is record the error and report it to the next <code>fsync</code> on that file, and then clear it.</p>\n<p>Which means the error is delivered once, to whoever asks first, and a program that retries the <code>fsync</code> after a failure gets success the second time. Not because anything was written, but because the error was already reported. Several databases treated that success as proof of durability and had lost data.</p>\n<p>The current guidance is blunt: an <code>fsync</code> failure is not retryable and the data it covered must be assumed lost. Some systems respond by treating it as fatal, which sounds extreme and is the only interpretation the interface supports.</p>\n<h2 id=\"barriers-and-the-order-nobody-promised\">Barriers, and the order nobody promised</h2>\n<p>One more thing the interface does not give you, because assuming it does is a common design error.</p>\n<p>Nothing orders two writes to different files, or even two writes to one file across a flush boundary. The kernel may write back pages in any order it likes, and the device may complete them in any order it likes, so a program that writes a record and then writes a pointer to it can lose the record and keep the pointer.</p>\n<p>That is exactly the corruption a journal exists to prevent, which is the next unit. The mechanism is always the same: write the thing, flush, and only then write the reference to it, so no crash can leave a reference to something that does not exist.</p>\n<p>The cost is the flush in the middle, which is the real device latency rather than the buffered one, and it is why a system doing many small durable updates is limited by flushes per second rather than by bandwidth. Batching several logical updates into one flush is the standard answer and it is why databases group commits.</p>\n<h2 id=\"going-around-it\">Going around it</h2>\n<p>Databases mostly do not want the page cache. They have their own buffer pool, they know their access pattern, and a second cache underneath theirs wastes memory holding a copy of what they already have.</p>\n<p>Opening a file with the direct flag skips the cache: reads come from the device and writes go to it. The price is that every transfer must be aligned, in offset, in length, and in the memory address of the buffer, usually to the device's block size. Get it wrong and the call fails rather than being slow.</p>\n<p>It also means every access pays full device latency, so it is only correct for a program that manages its own caching and its own read-ahead. For everything else the page cache is better than what you would write, and the reason to avoid it is duplication rather than overhead.</p>\n<h2 id=\"mapping-is-not-a-shortcut\">Mapping is not a shortcut</h2>\n<p>Unit 035 described <code>mmap</code>, and it is worth stating what it does and does not change here.</p>\n<p>Mapping a file makes reads faults rather than system calls, which removes the transition and the copy. That is a real saving for a workload reading the same data repeatedly.</p>\n<p>It does not change durability. A write through a mapping dirties a page exactly as <code>write</code> does, and it needs the same flush. It does not change the eviction policy either, so a mapped file competes for memory with everything else.</p>\n<p>And it introduces a failure mode the read path does not have: an I/O error on a mapped page is a signal rather than a return value, which almost no program is prepared to handle.</p>\n<h2 id=\"what-to-carry-forward\">What to carry forward</h2>\n<p>Reads and writes talk to memory, so a write returning means the page is dirty and nothing more.</p>\n<p>Read-ahead makes sequential access fast and random access unhelped, dirty page limits explain a program that suddenly slows to device speed, and durability is <code>fsync</code> on the file and on the directory.</p>\n<p>And an <code>fsync</code> error is reported once and then cleared, so a retry that succeeds proves nothing.</p>\n<h2 id=\"reading-the-errors-you-are-about-to-see\">Reading the errors you are about to see</h2>\n<p>These model the cache's decisions, which is exact, where a measurement on a shared machine would depend on what everybody else had recently read.</p>\n<p><code>assert-failed</code> names the outcome your model got wrong. Several of the exercises assert that something is not durable, which is the point of them: a model that reported durability after a plain write would be modelling a system nobody has.</p>",
 "headings": [
  {
   "id": "the-cache-is-the-memory-system",
   "text": "The cache is the memory system",
   "level": 2
  },
  {
   "id": "guessing-what-comes-next",
   "text": "Guessing what comes next",
   "level": 2
  },
  {
   "id": "when-a-write-finally-blocks",
   "text": "When a write finally blocks",
   "level": 2
  },
  {
   "id": "the-only-thing-that-makes-it-durable",
   "text": "The only thing that makes it durable",
   "level": 2
  },
  {
   "id": "the-error-you-only-get-once",
   "text": "The error you only get once",
   "level": 2
  },
  {
   "id": "barriers-and-the-order-nobody-promised",
   "text": "Barriers, and the order nobody promised",
   "level": 2
  },
  {
   "id": "going-around-it",
   "text": "Going around it",
   "level": 2
  },
  {
   "id": "mapping-is-not-a-shortcut",
   "text": "Mapping is not a shortcut",
   "level": 2
  },
  {
   "id": "what-to-carry-forward",
   "text": "What to carry forward",
   "level": 2
  },
  {
   "id": "reading-the-errors-you-are-about-to-see",
   "text": "Reading the errors you are about to see",
   "level": 2
  }
 ],
 "words": 1429,
 "needs": [
  "virtual-memory",
  "flash"
 ],
 "neededBy": [
  "filesystems"
 ]
}
