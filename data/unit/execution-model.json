{
 "slug": "execution-model",
 "num": 89,
 "title": "The execution model",
 "blurb": "The warp is the unit of execution and the block is the unit of cooperation. Neither is the thread.",
 "part": "gpu",
 "partRoman": "XVI",
 "partTitle": "Graphics and the GPU",
 "accent": "phos",
 "backend": "modal",
 "meta": {
  "needs": [
   "sm-shape",
   "throughput"
  ],
  "minutes": "55",
  "one_idea": "The warp is the unit of execution and the block is the unit of cooperation. Neither of them is the thread.",
  "sources": [
   "modal-gpu-glossary",
   "cuda-programming-tuning",
   "graphics-pipeline"
  ]
 },
 "html": "<p>You launch a kernel with <code>&lt;&lt;&lt;blocks, threads&gt;&gt;&gt;</code> and write code that reads as though each thread runs on its own. That reading is the CUDA programming model, it is deliberate, and it is not what the hardware does.</p>\n<p>Two facts sit underneath it. Threads are executed in groups of 32 called warps, and warps do not appear in the programming model at all. Blocks are the unit within which threads may cooperate, and between blocks there is no cooperation and no ordering whatsoever.</p>\n<p>Almost everything that surprises people about GPU code follows from one of those two.</p>\n<h2 id=\"where-32-comes-from\">Where 32 comes from</h2>\n<p>The number is not a round figure someone chose. Fragments are shaded in 2x2 quads because mipmap selection needs a screen-space derivative and the cheapest derivative is a difference against a neighbour. NVIDIA's own Tesla paper puts it plainly: the SM controller groups eight pixel quads into a warp of 32 threads. Eight quads, four pixels each.</p>\n<p>So the width of a warp is a fact about texture filtering, inherited by a compute API that has nothing to do with graphics. It is the clearest single piece of evidence that a GPU is a rasteriser that got general enough to do arithmetic.</p>\n<p>Two consequences that matter immediately. A block of 33 threads occupies two warps and wastes 31 lanes of the second. And <code>warpSize</code> is a runtime value rather than a compile-time constant, which matters the moment you read code written for a different vendor, where the same concept is 64 wide.</p>\n<h2 id=\"divergence-costs-both-branches\">Divergence costs both branches</h2>\n<p>A warp has one instruction pointer for 32 lanes. When lanes disagree about which way a branch goes, the hardware does not run two branches at once. It runs one side with the disagreeing lanes masked off, then the other side with the mask inverted.</p>\n<pre class=\"cb\" data-lang=\"text\"><code>if (threadIdx.x % 2 == 0) a();   // 16 lanes active, 16 idle\nelse                      b();   // 16 lanes active, 16 idle\n                                 // total time: a() + b()</code></pre>\n<p>Both paths cost their full time and half your lanes sit idle through each. That is why divergence is described as expensive: not because branching is slow, but because the work does not overlap.</p>\n<p>The important qualifier is that divergence is <strong>spatial</strong>, not statistical. A branch that 50% of your threads take is free if the split lands on warp boundaries, and ruinous if it alternates every lane. A CPU cares whether a branch is predictable in time; a GPU cares whether it is uniform in space.</p>\n<p>There is a further wrinkle worth knowing before you read old code. Before Volta, lanes in a warp shared a program counter and reconverged in lockstep, and a great deal of code quietly relied on it. From Volta onward each thread has its own program counter and its own call stack, so lockstep is no longer promised. This is why the warp intrinsics all carry a <code>_sync</code> suffix and an explicit mask: you now have to say which lanes you are talking about, because the hardware no longer guarantees they arrive together.</p>\n<h2 id=\"a-block-is-a-promise-about-cooperation\">A block is a promise about cooperation</h2>\n<p>Threads within a block can see the same shared memory and can wait for each other at <code>__syncthreads()</code>. That is what a block is for.</p>\n<p>Between blocks there is nothing. No shared memory, no barrier, and no ordering: a correct kernel must produce the right answer for every possible interleaving of its blocks, including one where they run strictly one after another in any order. A block that waits on another block deadlocks, because the block it is waiting for may not have been scheduled yet and may not be scheduled until the waiting block finishes.</p>\n<p>This looks like a restriction and it is actually the mechanism that makes your code portable. Because blocks are independent, the same kernel runs on a GPU with 20 SMs and on one with 148 without recompilation, and gets faster on the larger one. Every scheduling decision is the hardware's, and it can only be the hardware's if you promised not to care about the order.</p>\n<p><code>__syncthreads()</code> has a rule people break: every thread in the block must reach it. Placing it inside a branch that only some threads take is undefined behaviour, and in practice hangs.</p>\n<h2 id=\"what-the-hardware-actually-keeps\">What the hardware actually keeps</h2>\n<p>It is worth being concrete about why a GPU can afford to have thousands of threads in flight, because the answer is not what a CPU background suggests.</p>\n<p>On a CPU, switching between threads is expensive: the registers belong to the core, so the operating system saves them, loads another set, and resumes. That costs hundreds to thousands of cycles, which is why you keep the number of runnable threads near the number of cores.</p>\n<p>An SM does the opposite. Its register file is large enough that every resident warp keeps its own registers for as long as it lives, so switching between warps saves and restores nothing at all. The scheduler picks a different warp that happens to be ready and issues from it on the next cycle. The cost is one cycle, and there is no state to move.</p>\n<p>That inverts the intuition completely. More threads do not cost more switching overhead; they are the mechanism by which a stall becomes free. When one warp is waiting several hundred cycles for a value from memory, the SM issues from another, and the wait disappears behind work that was going to happen anyway.</p>\n<p>The price is paid elsewhere, and you should know where. The register file is finite, so the registers each thread uses limit how many warps can be resident, and using too many quietly reduces the number of warps available to hide latency. That trade has its own unit later. For now the point is that the register file is a context store, not a cache, and it is the reason the whole model works.</p>\n<h2 id=\"talking-within-a-warp-without-memory\">Talking within a warp without memory</h2>\n<p>Because a warp executes together, lanes can exchange registers directly, with no memory and no barrier:</p>\n<pre class=\"cb\" data-lang=\"text\"><code>val += __shfl_down_sync(0xffffffff, val, 16);\nval += __shfl_down_sync(0xffffffff, val, 8);\nval += __shfl_down_sync(0xffffffff, val, 4);\nval += __shfl_down_sync(0xffffffff, val, 2);\nval += __shfl_down_sync(0xffffffff, val, 1);</code></pre>\n<p>Five steps, and lane 0 holds the sum of all 32. That is log2(32), and it uses no shared memory at all. The mask <code>0xffffffff</code> names all 32 lanes; if some lanes have already exited, naming them is undefined behaviour, so the mask is a claim you have to be able to make honestly.</p>\n<p>This register-exchange path is a direct descendant of the crossbar built so fragment shaders could compute derivatives against their quad neighbours. The same wires, exposed to a different API.</p>\n<h2 id=\"indexing-and-the-loop-you-should-write-instead\">Indexing, and the loop you should write instead</h2>\n<p>The idiom every kernel starts with:</p>\n<pre class=\"cb\" data-lang=\"text\"><code>int i = blockIdx.x * blockDim.x + threadIdx.x;\nif (i &lt; n) out[i] = f(in[i]);</code></pre>\n<p>The guard is not optional. Your grid is a whole number of blocks, so unless <code>n</code> divides <code>blockDim.x</code> exactly there are threads past the end of the data, and they will happily write past the end of your array.</p>\n<p>A grid-stride loop is usually better:</p>\n<pre class=\"cb\" data-lang=\"text\"><code>for (int i = blockIdx.x * blockDim.x + threadIdx.x;\n     i &lt; n; i += blockDim.x * gridDim.x) { ... }</code></pre>\n<p>It decouples the grid size from the problem size, so one launch configuration works for every <code>n</code>, the same kernel can be tuned for occupancy independently of the data, and it is far easier to debug because you can run it with a single block and a single thread and get the same answer.</p>\n<h2 id=\"what-to-carry-into-the-next-units\">What to carry into the next units</h2>\n<p>Three sentences.</p>\n<p>The warp is the unit of execution, so ask of any branch whether it divides lanes or divides warps. The block is the unit of cooperation, so ask of any communication whether it stays inside one. And the thread is the unit you write, which is a convenience the hardware provides rather than a description of it.</p>\n<p>Every performance idea in the rest of this part is one of those three, examined more closely.</p>\n<h2 id=\"reading-what-the-exercises-tell-you\">Reading what the exercises tell you</h2>\n<p>These exercises run on a real GPU that you rent, so the failures they report are the ones you would get on a cluster rather than approximations of them.</p>\n<p>A compile error from <code>nvcc</code> names a file and a line the way <code>gcc</code> does, with one difference worth knowing now: nvcc has no stable error codes. Its warnings are numbered, as <code>#549-D</code>, and its errors are prose. So an explanation here is keyed to the shape of the message rather than to an identifier, and the build checks that the shape still matches whenever the toolkit moves.</p>\n<p>A kernel that launches and then misbehaves usually says nothing at all. There is no exception, no stack trace, and often no wrong output either, because a kernel that writes out of bounds may corrupt something you are not printing. The exit status is your first signal and the sanitizer is your second.</p>\n<p>The most instructive failure in this unit is the one where everything succeeds. The compiler is happy, the kernel runs, the program exits 0, and the number is wrong because a warp did not do what you assumed it did. Those exercises are marked, and there is prose waiting for exactly that outcome.</p>",
 "headings": [
  {
   "id": "where-32-comes-from",
   "text": "Where 32 comes from",
   "level": 2
  },
  {
   "id": "divergence-costs-both-branches",
   "text": "Divergence costs both branches",
   "level": 2
  },
  {
   "id": "a-block-is-a-promise-about-cooperation",
   "text": "A block is a promise about cooperation",
   "level": 2
  },
  {
   "id": "what-the-hardware-actually-keeps",
   "text": "What the hardware actually keeps",
   "level": 2
  },
  {
   "id": "talking-within-a-warp-without-memory",
   "text": "Talking within a warp without memory",
   "level": 2
  },
  {
   "id": "indexing-and-the-loop-you-should-write-instead",
   "text": "Indexing, and the loop you should write instead",
   "level": 2
  },
  {
   "id": "what-to-carry-into-the-next-units",
   "text": "What to carry into the next units",
   "level": 2
  },
  {
   "id": "reading-what-the-exercises-tell-you",
   "text": "Reading what the exercises tell you",
   "level": 2
  }
 ],
 "words": 1555
}
